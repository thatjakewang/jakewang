# The packages this app chose. The fully pinned set (these plus every
# sub-dependency) lives in requirements.lock — install that one everywhere.
# Regenerate it with:  uv pip compile requirements.txt -o requirements.lock
fastapi==0.136.1
uvicorn==0.46.0
SQLAlchemy==2.0.49
psycopg2-binary==2.9.12
pydantic-settings==2.14.2
slowapi==0.1.9
Jinja2==3.1.6
